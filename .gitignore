node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example

# rolepod-wplab artifacts (per-project runs)
.rolepod-wplab/

# Fixture WP install (regenerated by tests/fixtures/install-wp.sh)
tests/fixtures/wp-data/

# Internal design brief — maintainer-only notes, never published.
# Reconstructable from conversation context if lost; not part of OSS surface.
brief/

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db

# Lock files (allow only npm by default; remove rule if switching)
yarn.lock
pnpm-lock.yaml
bun.lockb

# Browser-driven UI verification artifacts (local-only, can be regenerated)
.rolepod-uiproof/

# Browser-driven UI verification artifacts (consolidated namespace, local-only)
.rolepod/
